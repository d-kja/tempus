{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for application windows",
  "windows": ["main", "settings", "new-entry", "edit-entry-*"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
